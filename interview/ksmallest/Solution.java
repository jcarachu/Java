class Solution {
	public static void main(String[] args) {
		Set<Integer> set = new HashSet<>();
	}
	
	public static int ksmallest(int[] arr, int k)
	{
		// Sorting = O(n^2) || O(nlgn)
		// Heap = O(nlgn)
		// Partitioning
		// (n-k) expected position
		int n = arr.length;
		int left = 0;
		int right = n -1;
		
		while (left <= right)
		{
			
		}
	}
}